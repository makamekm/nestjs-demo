{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "prebuild": "rimraf ./packages/api/public",
    "build": "run-s prebuild build:packages build:dist",
    "build:packages": "lerna run build",
    "build:dist": "ncp ./packages/ui/build ./packages/api/public",
    "start": "lerna run start:prod",
    "start:dev": "lerna run start"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "npm-run-all": "^4.1.5",
    "ncp": "^2.0.0",
    "rimraf": "^3.0.2"
  }
}
